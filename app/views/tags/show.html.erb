<h1>[Tags - Show Page]</h1>

<h2><%= @tag.name %></h2>

<% @tag.pictures.uniq.each do |p| %>
	<%= link_to image_tag(p.url, size: "300x300", class: "grid"), p %>
<% end %>

<hr/>

<%= link_to 'All Tags', tags_path %>
